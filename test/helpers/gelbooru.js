"use strict";

const assert = require("assert"),
    chai = require("chai"),
    expect = chai.expect,
    should = chai.should();

describe("Gelbooru", () => {
    describe("posts_list", () => {
        it("should", () => {
            assert(1 === 1);
        });
    });
});
