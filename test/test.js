"use strict";

const assert = require("assert"),
      chai = require("chai"),
      expect = chai.expect,
      should = chai.should();

it('dummy-test', () => {
    const success = true;

    expect(success).to.be.true;
});